[package]
name = "talaria-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Control-socket protocol types shared between the Talaria shell and its MCP proxy"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
