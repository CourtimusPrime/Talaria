# Overnight test-and-improve loop — 2026-08-15

Branch: `overnight/2026-08-15`. Started 01:39 +04. Hard stop: 8 hours (~09:40) or 25 iterations.

## Needs your call

*(judgment calls surfaced during the loop land here — none yet)*

- **Note, not a bug:** the mission brief says "Tauri-shelled" with "React chrome". The current build is a winit shell with egui chrome, per SPEC's own dependency-status finding (tauri-runtime-verso dormant since 2025-10; custom winit embedder until the 16-weeks-out re-check). Accessibility work below targets the egui chrome accordingly. Not redesigning this tonight.

## Summary

- Issues found: (running)
- Fixed: (running)
- Reverted: 0

## Log

*(one line per iteration: issue → fix → verified how)*
1. [agent-use] No end-to-end test of the real MCP stdio path existed → added tests/e2e/mcp_client_test.py driving talaria-mcp as a real client (initialize, tools/list, all 9 tools incl. error paths) + vendored control-socket e2e into tests/e2e/ → verified: full pass against live shell under Xvfb (owner labeled "mcp-e2e", download lands in ~/Downloads, bad-tab returns isError).
2. [stability] Screenshot of a non-displayed tab returned the displayed tab's pixels (hidden webview's pipeline is throttled; its paint() never reaches the shared framebuffer) → replaced sync capture with a pending-capture queue: hide displayed, show target, wait for notify_new_frame_ready (or 1.5s timeout), then paint + read_to_image + restore; WaitUntil scheduling wakes the loop for timeouts; frame-ready flag set inside the delegate (no painting there — painter-borrow re-entrancy) → verified: agent-screenshot.png now shows example.com (54KB, was byte-identical servo.org before); control-socket e2e + full MCP client regression both pass. Side effect: displayed tab may briefly show the captured tab (<1.5s) during background capture.
3. [stability] Crash-recovery path existed only as a crashed:true flag (no UI, agents could still call evaluate/screenshot into a dead tab) → crashed tabs now render an "Aw, Snap"-style panel with a Reload button; evaluate/screenshot on a crashed tab return a tool error; navigate (and Reload) clear the flag and recover; added TALARIA_TEST_HOOKS=1-gated crash-sim evaluate hook + tests/e2e/crash_recovery_test.py → verified: full pass (crash → crashed=true in tabs_list → errors → navigate recovers → evaluate works again) + crashed UI confirmed by screenshot. Follow-up (not user-call): pushing Event::TabCrashed to connected control clients is still unwired; agents currently learn via tabs_list/tool errors.
4. [speed] Baseline measured, no fix needed: control socket ready 21ms after exec, first command reply 72ms, example.com loaded 219ms, displayed-tab screenshot 4–5ms (takeover target is 30–60ms — beaten by ~10x; local viewing is composited in-process, no polling at all). RSS after one tab: 303MB — that's Servo's baseline footprint, engine-level, not actionable in the shell (logged, not touched per guardrails).
5. [accessibility] Chrome was unreachable by keyboard (keys went to the page unless egui already had focus) → added standard shortcuts intercepted ahead of both egui and the page: Ctrl+L focuses+navigates URL bar, Ctrl+T new tab, Ctrl+W close tab, Ctrl+R/F5 reload; ModifiersChanged now also feeds egui → verified: xdotool-driven tests/e2e/keyboard_nav_test.py passes (tab count 1→2→1, URL navigated); control-socket e2e regression passes.
6. [tests] Vendored e2e runner still invoked the old e2e.py filename (renamed to control_socket_test.py), so the "regression pass" in iteration 5 was a file-not-found exit 2, not a real run → fixed the runner path → verified: full control-socket e2e passes; also removes stray test artifacts from the repo tree (screenshots land in tests/e2e when run from repo — added to .gitignore).
7. [stability] An `evaluate` whose script never terminates left the MCP client hanging forever (servo never fires the completion callback) → control connection now times out command replies (default 30s, TALARIA_COMMAND_TIMEOUT_SECS override) and returns a tool error; the connection stays usable afterwards. Also verified the session-end path: agent tabs persist with their owner label after the client disconnects, per the shared-session design → verified: tests/e2e/timeout_session_test.py (3s test timeout: error reply in 3.0s, tabs_list works after, tab survives disconnect). Note: the page's script thread remains wedged — Servo has no slow-script interrupt; closing the tab is the recovery.
8. [agent-use] tabs_open/navigate rejected scheme-less URLs ("example.com" → bad url) → agent URLs now assume https:// for host-shaped input; garbage still errors; never falls back to search (that's omnibox-only behavior) → verified: live socket test (open "example.com" → https://example.com/, navigate "servo.org" ok, "not a url at all" still rejected).
9. [design] Toolbar glyphs (← → ⟳ ＋ ✕) rendered as boxes — egui's default font lacks those codepoints → switched to ASCII labels (< > R + x) until a proper icon font ships; added a loading spinner next to the URL bar driven by WebView::load_status() (hidden for crashed tabs) → verified: chrome screenshot shows legible buttons; e2e URL checks pass.
10. [stability] Every launch logged ClientStorage sqlite failures ("unable to open database file /tmp/.tmpXXX/...") — servo's default Opts uses a temp profile dir, so localStorage was broken AND all session state died with the process (directly against the shared-session/cookie-reuse design) → set Opts.config_dir to ~/.config/talaria/servo (created on launch) → verified: localStorage.setItem survives a full shell restart; zero ClientStorage errors in the log; control-socket e2e regression passes.
11. [agent-use] Concurrency + JSON audit: two agent sessions hammering evaluate concurrently (15 calls each, interleaved) all return correct results; complex objects/arrays round-trip; DOM handles come back as "[element <id>]" strings; undefined object fields are dropped (JSON semantics). One fix: integral JS numbers serialized as "42.0" — now emitted as "42" (floats stay floats) → verified: live evaluate returns {"i": 42, "f": 1.5}.
12. [stability/CORE] Takeover was silently broken: switching to the Agents view changed the chrome but the content pane kept showing the Me tab (a webview that is already marked shown never produces a fresh frame, so paint() no-ops and the shared framebuffer stays stale — earlier "Agents view works" screenshots were misread, only the chrome had switched). Root fix: TabManager now enforces "exactly the displayed tab is shown/focused, all others hidden" (sync_visibility) so every view/tab switch is a hidden→shown transition that forces a real frame. Also: link clicks did nothing anywhere — the default request_navigation delegate drops the request; now allowed unconditionally (no-gatekeeping) → verified: tests/e2e/takeover_test.py — real mouse click on a link inside the agent's tab navigates it to iana.org with content pane, URL bar, and title all updating; full regression (control-socket, crash, timeout, MCP client) green.
