# Overnight test-and-improve loop — 2026-08-16

Branch: `overnight/2026-08-16`. Started 09:34 +0400. Hard stop: 8 hours or 25 iterations.
Baseline: main after the four close-outs (crash-event push, per-webview framebuffers, Phosphor icons, VoiceOver checklist) on top of the merged 2026-08-15 loop.

## Working-tree conflict investigation (12:55, on request — no resolution taken)

**Verdict on the `loading: bool` field:** same idea, superseded — not a conflict, not additive. The working tree has **no** uncommitted diff on `talaria-protocol/src/lib.rs` now (`git diff` empty). The uncommitted field that needs-your-call #1 saw at ~11:20 was step one of iteration 6, which this session completed and committed as `9bb64c5` (11:5x–12:0x, "Wait for page load in tabs_open/navigate; report loading state") with the identical field name and doc comment, plus the shell side (`PendingLoad`, `tabs_list`/`tabs_open`/`navigate` reporting). `git show 9bb64c5:crates/talaria-protocol/src/lib.rs` lines 103–108 are byte-identical to today's lines 107–112 (offset only from the later `open_for_user` variant in `1885d49`).

**Root cause of the flag: two Claude sessions on one working tree and branch.** Session A (the loop-2 owner: started 09:34, soak `soak.py` pid 1854309 running on `:99` since 09:36, job `fe020d5b`, last state write 12:20) saw session B's half-typed edit and committed `411072a` at 11:24 ("freeze shell rebuilds this loop"). Session B (this one: started 11:17 with the brief "kick off another overnight loop, branch overnight/2026-08-16") found the branch already started and resumed it as its own — 11 commits since (`3d1001b`…`9107dca`, iterations 6–12) and it rebuilt `target/release/talaria` eight times. Consequences to know about:
- A's "remaining activity uses the already-built binary" no longer holds — its final regression will run against B's latest build (all of B's builds passed `run_all` 8/8 on `:98`, so it should still pass, but it is not the binary A thinks it is).
- B's two needs-your-call additions from 12:07 were **silently dropped**: B's log edit string-replaced `*(none yet)*`, which A had already replaced in `411072a`, so the replace matched nothing. Restored here so they are not lost:
  - *Agents can open `file://` URLs.* `tabs_open`/`navigate` accept any scheme Servo loads; Servo renders local files with recognised extensions (`.html`, `.txt`, …), so an agent can read local files via `evaluate` (`file:///etc/hostname` only came back as an octet-stream error page for lack of an extension). Maybe intended (open a local HTML report) — or restrict agent-supplied URLs to `http(s)`/`about:`/`data:` (one-line policy in `parse_agent_url`; human URL bar unaffected). Not decided.
  - *Heavy-JS pages wedge `evaluate`.* On github.com every `evaluate` after load — even `document.title` — hits the 30s command timeout (Servo's script thread busy with the page's own JS; nothing shell-side to fix, a Servo slow-script interrupt would be the real fix). Agents get the timeout error and can move on.
- The e2e launchers now kill only Talaria processes attached to their own display (`tests/e2e/harness.py`); the default display is still `:99`, which is A's soak display — so A running the standalone suites still ends its own soak exactly as before, no new hazard, but no protection either unless it sets `TALARIA_E2E_DISPLAY`.

**`.README.md.swp` — not checked before removal, reporting that plainly:** this session ran `rm -f .README.md.swp` at ~12:06 (just before commit `3d1001b`), treating it as a stray, without reading its PID field. The file is gone, so that check cannot be done now. Evidence available after the fact: no `vim`/`nvim`/`emacs` process exists (only the `opencode web` server from Aug 13), `lsof README.md` is empty, and README.md was rewritten by this session at 12:0x and 12:44 with no interference — consistent with a stale swap from an earlier editor, but that is inference, not verification.

**Other uncommitted state right now (left untouched):** `crates/talaria-shell/src/app.rs` + `crates/talaria-shell/src/tabs.rs` are this session's iteration-13 work in progress — `window.open` / `target=_blank` support via `WebViewDelegate::request_create_new` (previously a silent no-op; built and probe-verified: popup lands as a tab in the parent's view with the parent's owner, evaluate/screenshot/close on it work) — not yet e2e-tested or committed. `.claude/settings.local.json` (rust-analyzer LSP plugin enable, untracked) also left alone. `.claude/HANDOFF-KzpAG.md` deletion was committed by this session as `f67c32f`.

**Stopped here as instructed.** The call needed: which session continues the loop (A is at soak-verdict/final-regression stage; B is mid-iteration 13), and whether B's iteration-13 popup change should be finished or dropped.

## Needs your call

1. **In-flight working-tree changes found mid-loop (~11:20)** — repo dir renamed open-browser→talaria, and `talaria-protocol/src/lib.rs` has an uncommitted `loading: bool` field on TabInfo (with block-until-loaded semantics sketched in its doc comment), shell side not yet implemented — the shell won't compile until that lands. Looks like your (or another session's) work in progress, so the loop did NOT touch, finish, or revert it, and stopped rebuilding the shell from this point; remaining loop activity uses the already-built binary (soak + final e2e regression are unaffected). Also present: `.claude/HANDOFF-KzpAG.md` deleted in the working tree and a `.README.md.swp` vim swap — left alone.

## Summary

- Iterations: (running)
- Issues found & fixed: (running)
- Reverted: 0

## Log

*(one line per iteration: issue → fix → verified how)*
1. [speed] Re-baseline after per-webview framebuffers: startup/socket 21ms, displayed capture 4–5ms, background capture 2–82ms (frame-ready path healthy) — all unchanged or better. New cost surfaced: ~12MB/tab (own framebuffer + page) and ~31MB allocator retention after closing 10 tabs; a heavy page (servo.org) costs ~200MB engine-side. Observation only — lazy framebuffer allocation is a possible future optimization, not tonight's redesign.
2. [stability] 200 rapid connect/hello/disconnect session churns (a third dying mid-request): RSS 303→304MB, thread count flat, sessions map drains, events still delivered to a fresh client afterwards — the new per-connection writer/event channels don't leak.
3. [speed] Idle CPU with a static page: 0.0% over a 20s window — ControlFlow::Wait sleeps properly, no repaint loop. Clean audit.
4. [design] Agents view listed agent tabs flat with a bare count → tabs now group under their owning session's label (Phosphor robot icon + client name), and tabs whose session disconnected fall into a "disconnected" group (tabs outlive sessions by design) → verified: screenshot with three concurrent MCP sessions shows per-session grouping. Follow-up noted (not done): pushing tab events as MCP notifications through the stdio proxy — socket-level events exist for distributed clients; MCP-side push is a separate design question.
5. [tests] keyboard_nav and takeover standalone suites raced a just-killed Xvfb when run back-to-back (bit the 08-15 final sweep) → they now wait for :99 to actually accept connections (xdpyinfo poll) and retry the window search → syntax-verified now; exercised in this loop's final regression (running them mid-soak would kill the soak's display). Soak checkpoint 50min: 153 cycles ok, RSS flat 477–478MB (+20MB vs loop 1 — the per-tab framebuffer, expected).
6. [agent-use] `evaluate` right after `tabs_open` returned an opaque Servo `InternalError` (no document yet); right after `navigate` it ran in the *previous* page (goto race) → `tabs_open`/`navigate` now reply once the new page reaches LoadStatus::Complete (20s cap) with the tab's final URL/title and a `loading` flag (also on `tabs_list`); a tab closed mid-load replies immediately with an error; evaluate errors now carry the script's message + line:col, and engine not-ready states say "page not ready — retry" → verified: probe (open 209ms w/ title; navigate→immediate evaluate sees new page; about:/data:/error pages resolve; unreachable host resolves at Servo's 15s connect timeout; double-navigate ordered) + control-socket e2e assertions + full run_all green. Also audited clean this iteration: protocol robustness (junk lines, 2MB line, request-before-hello, unknown command, duplicate hello — all error replies, connection stays usable), screenshot-then-close race, error-page semantics.
7. [agent-use] Promises came back as `{}` and top-level `await` failed to compile — async agent scripts (fetch, waits) silently broken → shell wraps scripts (indirect eval keeps global semantics; thenables parked on `window.__talaria_async[slot]` and polled from the event loop until settled or the command timeout; top-level await retried as `return (script)` then as an async body); Servo enforces CSP eval-blocking via the SpiderMonkey callback, so CSP pages fall back to a raw run (works, promises not awaited there) → verified: 30-case probe matrix (sync/async/reject/throw/syntax/timeout/globals/`this`/unicode) + e2e (promise, `await fetch`, multi-statement return, rejection, globals persist, CSP page via local server) + full run_all green.
8. [tests] Standalone suites hardcoded :99 + `pkill -x talaria` (killed the soak; loop 1 had to defer them) → shared `tests/e2e/harness.py` (TALARIA_E2E_DISPLAY, private XDG_RUNTIME_DIR socket, kills only shells on its display) + `run_all.py` single-entry regression; the real back-to-back Xvfb race was a stale `/tmp/.X<n>-lock` whose owning Xvfb was a killed-but-unreaped zombie → launcher clears it and reaps → verified: run_all 7/7 green on :98 while the soak kept running on :99. Gotcha recorded: AF_UNIX paths >107 chars fail — keep XDG_RUNTIME_DIR short.
9. [stability] Second `talaria <url>` launch unconditionally re-bound the control socket: agents' new connections landed in the second window and, once it quit, the path was dead while the first shell lived on (unreachable until restart); both also shared one Servo profile → launcher now probes the socket first; a live shell gets an `open_for_user` command (Me tab, window focused, replies after load) and the second process exits 0 with a notice — standard single-instance browser behaviour; stale socket file → normal start → verified: second launch exits in 0.2s, tab appears in Me view, socket still served; kill -9 + restart over the stale file binds in 0.2s; new tests/e2e/single_instance_test.py in run_all (8/8 green).
10. [speed] Animated WebGL page (twgl-tunnel) under Xvfb: rAF runs at 10–45 fps and CPU is 100–640% (soak: ~10 cores) — every hot thread is Mesa `llvmpipe` (software GL); the shell's main thread sits at ~17% while the animation runs, control/script threads idle. Nothing shell-side to fix; on a real GPU this is negligible. Observation only. Also measured screenshot encode: PNG default ≈30ms on a busy 1200×755 frame vs ≈9ms with `Compression::Fast` at +50% bytes — capture path already inside SPEC targets, left as is.
11. [design/stability] Window title only updated from page-title events (and only for the displayed tab), so tab switches, closes, Me/Agents toggles and "no tab" all showed the previous page's title → title now derived from the displayed tab each frame ("Tab crashed" / plain "Talaria"), set only on change → verified: `xdotool getwindowname` tracks Ctrl+T (servo.org) → Ctrl+Tab (example.com) → Ctrl+W → last close ("Talaria") → Agents view (agent tab's title). Also silenced the two long-standing compiler warnings.
12. [accessibility] Keyboard-only chrome audit: Tab/Shift+Tab traverse toolbar controls once the bar has focus (Shift+Tab → New tab → Enter opens a tab ✓); but Ctrl+L left the caret at the end (typing appended to the URL), Escape kept half-typed junk in the bar, and back/forward had no keys → Ctrl+L selects the URL (TextEditState cursor range), Esc reverts to the page URL and returns focus to the page, Alt+Left/Right = back/forward → verified: toolbar screenshots (selection highlight; reverted bar), keyboard_nav_test extended (alt+left/right) and passing; README keymap updated.

